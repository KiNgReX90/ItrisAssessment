package com.example.backend.service;

import org.springframework.stereotype.Service;

@Service
public class AccountService {
    // Account, being an entity that is always associated with a user. However, not every user does need to have an account.
    // Logic regarding account information, login, verification etc will be implemented here.
}